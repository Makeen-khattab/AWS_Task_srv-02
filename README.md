# AWS_Task_srv-02
This code is for an **HTTP service** launched over an **instance** running on **AWS cloud** which listens over a port and respond with the **html page** on having an **http request** from a client.
